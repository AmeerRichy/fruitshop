"use client";import{useEffect,useState}from"react";import{useSearchParams}from"next/navigation";import{StatusView}from"../../components/OrderLookup";export default function OrderClient({id}:{id:string}){const sp=useSearchParams(),[o,setO]=useState<any>(),[err,setErr]=useState('');useEffect(()=>{fetch(`/api/orders/${id}?phone=${encodeURIComponent(sp.get('phone')||'')}`).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error);return d}).then(d=>setO(d.order)).catch(e=>setErr(e.message))},[id,sp]);return err?<p className="py-20 text-center">{err}</p>:o?<StatusView order={o}/>:<p className="py-20 text-center">Loading order…</p>}
