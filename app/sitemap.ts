import type{MetadataRoute}from"next";import{getDb}from"./lib/db";export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_SITE_URL||'http://localhost:3000';const staticRoutes=['','/shop','/about','/contact','/track-order','/terms','/privacy','/refund-policy','/delivery-policy'].map(x=>({url:base+x,lastModified:new Date()}));try{const products=await(await getDb()).collection('products').find({archived:{$ne:true}}).project({slug:1,updatedAt:1}).toArray();return[...staticRoutes,...products.map(p=>({url:`${base}/product/${p.slug}`,lastModified:p.updatedAt||new Date()}))]}catch{return staticRoutes}}
